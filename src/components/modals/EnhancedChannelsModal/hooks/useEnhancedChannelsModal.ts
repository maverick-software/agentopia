import type { EnhancedChannelsModalProps } from '../types';

export function useEnhancedChannelsModal(
  props: EnhancedChannelsModalProps,
): EnhancedChannelsModalProps {
  return props;
}
import type { EnhancedChannelsModalProps } from '../types';

export function useEnhancedChannelsModal(props: EnhancedChannelsModalProps): EnhancedChannelsModalProps {
  return props;
}
